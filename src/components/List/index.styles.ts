import styled from 'styled-components/native';
import { styl } from '@/shared/Theme';

export const ListWrapper = styl(styled.ListView)`
`;

export const Title = styl(styled.Text)`
  font-size: 20px;
  padding: 10px 10px 0;
`;

export const Subtitle = styl(styled.Text)`
  font-size: 15px;
  padding: 10px;
  color: #555;
`;
